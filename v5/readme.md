# Testing WebdriverIO V5
## Initial setup
* Installing the dependencies and launching the standalone selenium server
```
npm install
```

* Launching a test
`node test`


* Run a specific test
`npm test -- --spec=tests/contactUs.js`

* Run the tests - Will use the wdio config file
`npm test`
